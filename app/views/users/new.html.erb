<h1>New User</h1>

<body>
<% if logged_in? == false %>
  <%= render "form", article: @users %>

  <br> 
  Or sign-in with an existing username

  <%= render "signin", article: @users %> 
<% else %>
  <%= button_to 'Log Out', logout_path, method: :delete %>

<% end %>


</body>

