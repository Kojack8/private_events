<h1>New User</h1>

<body>
<% if logged_in? == false %>
  <%= render "form", article: @users %>

  <br> 
  Or sign-in with an existing username

  <%= render "signin", article: @users %> 
<% else %> 
  <%= "You are already signed in as #{current_user.name}. You must sing out to create a new user name."%>

<% end %>


</body>

