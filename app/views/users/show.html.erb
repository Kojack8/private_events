<body>
<br><br><br>

<div class = 'event_block'>
  Upcoming Events (Host):
  <div class = 'event_block_small'>
    <% @user.events.upcoming.each do |event| %>
      <div class = 'title_block'>
          <%= link_to event.title, event_path(event) %> 
        </div>
        <div class = 'hosted_block'>
          <%= "Hosted by: #{event.host.name}"%>
        </div>
        <div class = 'time_block'>
        <%= "To be held: #{event.time.strftime("%A, %B %e, %Y at %l:%M%p.")}"%>
        </div>
      </div>
      <br>
    <% end %>
  </div>
</div>
<br>

<div class = 'event_block'>
  Upcoming Events (Guest):
    <% @user.attended_events.upcoming.each do |event| %>
    <div class = 'event_block_small'>
      <div class = 'title_block'>
        <%= link_to event.title, event_path(event) %> 
      </div>
      <div class = 'hosted_block'>
        <%= "Hosted by: #{event.host.name}"%>
      </div>
      <div class = 'time_block'>
        <%= "To be held: #{event.time.strftime("%A, %B %e, %Y at %l:%M%p.")}"%>
      </div>
    </div>
    <br>
    <% end %>
</div>
<br>

<div class = 'event_block'>
  Past Events:
  <% @user.attended_events.past.each do |event| %>
    <div class = 'event_block_small'>
      <div class = 'title_block'>
        <%= link_to event.title, event_path(event) %> 
      </div>
      <div class = 'hosted_block'>
        <%= "Hosted by: #{event.host.name}"%>
      </div>
      <div class = 'time_block'>
        <%= "To be held: #{event.time.strftime("%A, %B %e, %Y at %l:%M%p.")}"%>
      </div>
    </div>
    <br>
    <% end %>
</div>


</body>