<h1>Events#index</h1>

<p>
  
  Upcoming Events:
  <br>
  <% @events.upcoming.each do |event| %>
    <%= link_to event.title, event_path(event) %>
    <br>
  <% end %>
  <br>
  Past Events:
  <br>
  <% @events.past.each do |event| %>
    <%= link_to event.title, event_path(event) %>
    <br>
  <% end %>

</p>
